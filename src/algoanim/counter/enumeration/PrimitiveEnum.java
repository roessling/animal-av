package algoanim.counter.enumeration;

/**
 * Enum class for communication between Primitive and CounterController
 * 
 * @author Axel Heimann
 */

public enum PrimitiveEnum {
	enqueue, dequeue, front, tail, push, pop, top, getData, getElement, put, swap, get, set
}
