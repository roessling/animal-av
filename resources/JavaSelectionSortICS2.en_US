@import=resources/GenericSelectionSort.en_US
algoName=Selection Sort (ICS2)
outputLanguage=Java
codeExample=public void selectionSort() {\n  int maxPosition, temp;\n  for(int i = a.length-1; i > 0; i--) {\n    maxPosition = i;\n    for(int j = 0; j < i; j++)\n      if(a[j] > a[maxPosition])\n        maxPosition = j;\n    temp = a[i];\n    a[i] = a[maxPosition];\n    a[maxPosition] = temp;\n  }\n}
animTitle=Selection Sort (ICS2)
algoTitle=Selection Sort (ICS2)
description.filename=resources/JavaSelectionSortICS2_descr_en.txt
code.filename=resources/JavaSelectionSortICS2_code.txt
complexity.filename=resources/JavaSelectionSortICS2_complexity_en.txt