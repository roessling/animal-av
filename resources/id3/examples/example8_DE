Beispiel: 
Du hast dein eigenes Beispiel erstellt! 

Nun wird ID3 einen Entscheidungsbaum erstellen, um die Klasse '§' 
vorherzusagen, mithilfe der Attribute '§', '§' und '§'. 
Sehen wir uns an, wie der Baum erstellt wird!

Er wurde basierend auf deinen Eingabedaten erstellt! 
So erhält man eine Vorhersage für ein neues Beispiel:
Mit dem Wurzelknoten beginnend folgt man den Kanten, die zu den entsprechenden Attributwerten eines Beispiels gehören; 
das Blatt, an dem man ankommt, gibt die Erwartung an (allerdings nur basierend auf den bisher gesehenen Beispiele).